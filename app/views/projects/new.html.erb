<div class="card">
    <div class="card-header">
        <h5><%= fa_icon_tag('map') %> <%= t('action.new_model', :model => mt(:project)) %></h5>
        <%= link_to fa_icon_tag('close'), projects_path, :class => 'card-header-close' %>
    </div>

    <div class="card-body">
        <%= form_for @project do |f| %>
            <%= render :partial => 'form', :locals => { :f => f } %>

            <div class="form-group mb-0">
                <%= f.submit t('action.submit'), :class => 'btn btn-submit' %>
                <%= link_to t('action.back'), projects_path, :class => 'btn btn-back' %>
            </div>
        <% end %>
    </div>
</div>
