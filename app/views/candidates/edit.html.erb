<div class="card">
    <div class="card-header">
        <h5><%= fa_icon_tag('user-circle') %> <%= t('action.edit_model', :model => mt(:expert)) %></h5>
        <%= link_to fa_icon_tag('close'), candidate_path(@candidate), :class => 'card-header-close' %>
    </div>

    <div class="card-body">
        <%= form_for @candidate do |f| %>
            <%= render :partial => 'form', :locals => { :f => f } %>
            <%= render :partial => 'form_work_exp' %>

            <div class="form-group mb-0">
                <%= f.submit t('action.save'), :class => 'btn btn-submit' %>
                <%= link_to t('action.back'), candidate_path(@candidate), :class => 'btn btn-back' %>
            </div>
        <% end %>
    </div>
</div>
<script>
//    addCandidatePhone();
//    addCandidateEmail();
</script>